RewriteEngine On
RewriteBase /Blog-Management-Syatem/

# If the URL is not a file or directory, forward it to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
